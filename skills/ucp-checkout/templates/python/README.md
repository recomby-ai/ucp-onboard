# Python Template

The current generator writes the FastAPI project from embedded templates in
`scripts/generate_api.py`. Keep this directory for future extracted templates
when the generated server grows.
